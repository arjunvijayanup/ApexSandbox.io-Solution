public String formatName(String firstName, String lastName) {
    //code here
    if (firstName && lastName) {
        return lastName + ', ' + firstName;
    }
    else if (firstName) return firsName;
    else if (lastName) return lastName;
    else ''
}